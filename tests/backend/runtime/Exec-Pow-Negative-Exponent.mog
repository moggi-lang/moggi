-- `^` at machine `Int` is the square-and-multiply primop, and a negative
-- exponent is the `ErrorCall` base raises: at the `^` expression itself, with
-- the enclosing statement as the outer frame, the same on every backend.
main :: IO ()
main = putStrLn (show (2 ^ (-1)))
