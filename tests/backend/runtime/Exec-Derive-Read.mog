import System.IO
import Text.Read
import Text.Show
import Data.Eq

data Color = Red | Blue | Green deriving (Eq, Show, Read)

main :: IO ()
main = do
  putStrLn (show ((read "Green") :: Color))
  putStrLn (show (((read "Red") :: Color) == Red))
