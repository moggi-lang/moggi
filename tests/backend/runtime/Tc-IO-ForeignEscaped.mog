module Tc.IO.ForeignEscaped

import System.Filesystem
import System.Filesystem.Path

isFileP :: FilePath -> IO Bool
isFileP p = isFile p

main :: IO ()
main = do
  let check = isFileP ("/tmp/moggi-tc-io-foreign.txt")
  b <- check
  pure ()
